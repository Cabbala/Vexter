# Vexter

Vexter is the Mac-side control plane for comparative analysis of `Cabbala/Dexter` and `Cabbala/Mew-X`. Git lives here; runtime, databases, logs, and replay data live on Windows via `ssh win-lan`.

## Fixed Environment Split

- Mac control plane: `/Users/cabbala/Projects/Vexter`
- Windows runtime host: `win-lan`
- Windows preferred root: `D:\Quant\Vexter`, with fallback to user space
- VPS: later-stage validation only

## Bootstrap Workflow

1. Run `./scripts/sync_reference_repos.sh` to clone or refresh the frozen Dexter and Mew-X branches into the local ignored `sources/` workspace.
2. Run `./scripts/bootstrap_windows_workspace.sh` from the Mac control plane to discover and prepare the Windows runtime/data layout.
3. Run `./scripts/recover_windows_runtime.sh` to restore PostgreSQL, detached frozen source checkouts, and the Dexter Python runtime on `win-lan`.
4. Run `./scripts/build_proof_bundle.sh` to refresh the task proof artifact bundle.
5. Run `pytest -q` before Git operations.
6. Complete Git operations from the Mac control plane so GitHub remains the source of truth, including branch, commit, push, and PR create/update.

## Operating Rules

- Every Codex task must leave GitHub in a state where ChatGPT can inspect the latest result directly. The minimum closeout path is branch, commit, push, and PR create/update.
- If agent or sub-agent capabilities are available and improve repo audit, environment verification, or implementation accuracy, Codex should use them proactively. If they are unavailable in the current session, proceed normally and record that limitation in artifacts.

## Repository Layout

- `docs/`, `specs/`, `ops/`, `plans/`: fixed guidance imported from the bootstrap bundle
- `manifests/`: committed source repo and environment manifests
- `config/`: committed planner/router config packages and frozen pins
- `scripts/`: Mac-side bootstrap and proof automation
- `artifacts/`: context pack, summary, proof manifest, task ledger, and bundles
- `tests/`: bootstrap validation checks plus planner/router unit tests
- `vexter/`: comparison helpers and the planner/router control-plane package

## Current Task

`TASK-001-source-assessment` is complete on `main`. The shared evaluation contract, source assessments, instrumentation plans, and Windows runtime layout are now the source of truth for Vexter.

`TASK-002-dexter-instrumentation` is complete on `main` as of 2026-03-21. The paired Dexter source branch adds observational NDJSON events, masked config export, leaderboard snapshots, and stagnant-mint replay exports without changing strategy logic.

`TASK-003-mewx-instrumentation` is complete on `main` after merging PR #3 on 2026-03-21. The paired Mew-X source branch adds observational NDJSON events, masked config export, candidate refresh snapshots, and session summaries without changing strategy or execution decisions.

`TASK-004-comparison-analysis` adds the first Vexter-side validator, shared metrics derivation layer, side-by-side comparison pack builder, and Windows collection runbook while keeping Dexter and Mew-X instrumentation frozen.

`TASK-005-live-comparison-evidence` remained blocked on the pre-paper promoted pair `resume-after-pr9-20260321T1737`, collected before Dexter `paper_live` validation. That baseline still sits at Dexter `4 / 12` and Mew-X `8 / 12`, so `TASK-006` stayed blocked and winner/tie decisions remained deferred.

`DEXTER-PAPER-DESIGN` records the completed design-only follow-up from `origin/main` at `0d4ff2e160feecd4313f0d8ff95d2ff084c5a7ee` (PR `#11`, merged on `2026-03-21`). It treats the blocker as Dexter coverage stall rather than repeated retry mechanics and documents a source-faithful minimal paper-mode / paper-equivalent path in `docs/dexter_paper_mode_design.md`. The design conclusion is to implement that minimal Dexter paper-equivalent path next, while keeping Dexter and Mew-X strategy logic, execution decisions, and instrumentation contracts unchanged during this design task and keeping `TASK-006` blocked.

`DEXTER-PAPER-IMPLEMENT` is now complete on `Cabbala/Dexter` `main` at `5dc1036c499af5f14f06d08ad0fa96aa36228c96` (PR `#2`, merged on `2026-03-21`). Dexter keeps `observe_live` as the default, adds an explicit opt-in `paper_live` execution path for comparison-safe entry/session/exit coverage, and leaves strategy semantics, trust logic, entry/exit thresholds, and Mew-X unchanged. Vexter now pins Dexter `main` at that merged commit for future sync/bootstrap work.

`TASK-005-PAPER-VALIDATION` re-verified `Cabbala/Vexter` `origin/main` through merged PR `#12` (`4b32053697fc4efddd76e542b3d2a1411b450240`) and PR `#13` (`379f55a50dbfd91968ba643e2fbfe5e73fad8392`), then recollected fresh matched Dexter `paper_live` x Mew-X `sim_live` evidence on `win-lan`. Two fresh same-attempt pairs (`task005-paper-validation-20260321T1950` and `task005-paper-validation-20260321T2005`) were rebuilt end to end. Dexter regressed to `1 / 12` required event types on both retries, emitting only `creator_candidate` despite explicit `paper_live`; Mew-X validated `9 / 12` on the stronger pair and `8 / 12` on the confirmatory retry. The second retry removed Dexter main-runtime startup `HTTP 429` without improving Dexter coverage, so the sharper blocker is now paper-live session conversion rather than generic startup recovery. `TASK-006` remains blocked.

`TASK-005-PAPER-REVALIDATE` re-verified `Cabbala/Vexter` `origin/main` at merged PR `#14` commit `3a720a64a5dbbda482513446b3bc47c341dde2ae` and `Cabbala/Dexter` `main` at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938` on `2026-03-21`. Two fresh same-attempt Dexter `paper_live` x Mew-X `sim_live` recollections (`task005-paper-revalidate-20260321T123011Z` and `task005-paper-revalidate-20260321T123426Z`) showed that Dexter is no longer stuck at creator-only coverage: both runs reached `mint_observed`, `entry_signal`, `entry_attempt`, `entry_fill`, `session_update`, `exit_signal`, `exit_fill`, and `position_closed`, lifting Dexter to `9 / 12`. Mew-X remained at `8 / 12` on both retries, and both comparison packs still validated only as `partial` because `candidate_rejected`, `entry_rejected`, and `run_summary` were still absent from the matched packages. The creator-only paper ceiling is resolved, but `TASK-006` remains blocked until a pass-grade matched pair is collected.

`TASK-005-PASS-GRADE-PAIR` re-verified `Cabbala/Vexter` `origin/main` at merged PR `#15` commit `28c15b5c7655fe7647c12774494c80b83c58c04f` and kept Dexter `main` pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938` with frozen Mew-X `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. A Vexter-side packaging regression in `scripts/collect_comparison_package.ps1` was fixed so single copied Mew-X session exports no longer crash the full-stream fallback path, then two fresh same-attempt Dexter `paper_live` x Mew-X `sim_live` recollections (`task005-pass-grade-pair-20260325T180027Z` and `task005-pass-grade-pair-20260325T180604Z`) were rebuilt end to end. The promoted pair improved to Dexter `10 / 12` and Mew-X `9 / 12`, but both fresh pairs still validated only as `partial`: Dexter missed `entry_rejected` and `run_summary` on both retries, while Mew-X still missed `creator_candidate`, `entry_rejected`, and `run_summary` on the strongest pair. Even the extended-grace confirmatory retry did not recover `run_summary`, so winner mode stayed deferred and `TASK-006` remains blocked.

`TASK-005-GAP-AUDIT` re-verified latest GitHub-visible Vexter `main` at merged PR `#16` commit `b71b5f744027024e2a04606f6c1aa369d0a6c3e3`, then revalidated the promoted and confirmatory pass-grade-pair packages against the current validator contract and the frozen Dexter / Mew-X sources. The audit closed in favor of a structural contract/source gap rather than a missing recollection recipe: `run_summary` is finalizer-only on both sources and the current matched-pair shutdown leaves both copied state exports in `running` state with no finalizer completion, `entry_rejected` is failure-path-only on both sources, and Mew-X `creator_candidate` depends on non-empty Deagle candidate reports even though both audited full-stream runs exported empty initial and refresh candidate snapshots. `TASK-006` therefore remains blocked, and the next logical step is validator contract audit / rule exception review rather than more same-shape recollection.

`TASK-005-VALIDATOR-CONTRACT-AUDIT` re-verified latest GitHub-visible Vexter `main` at merged PR `#18` commit `b7c8cb900ced104f4fe76cfd9ca48c2b21b82d81`, with supporting prior states PR `#17` `b09033d6c8ce21510da23025cee935e46f3ef4df` and PR `#16` `b71b5f744027024e2a04606f6c1aa369d0a6c3e3`. It confirmed that helper hardening alone is not a full unblock: `run_summary` sits on the shutdown-success boundary and may be helper-recoverable, but `entry_rejected` remains failure-path-only on both frozen sources and Mew-X `creator_candidate` remains conditional on non-empty candidate reports. The resulting outcome was `rule review needed`, so `TASK-006` stayed blocked and the approval-grade next step became a validator rule review / exception memo.

`TASK-005-VALIDATOR-RULE-REVIEW` rechecked the same GitHub-visible basis on `2026-03-26` without changing frozen source logic or validator rules. The resulting memo classifies `run_summary` as `shutdown_success_dependent`, `entry_rejected` as `failure_path_only`, and Mew-X `creator_candidate` as `source_specific_conditional`, then recommends proposal-level contract treatment for each: make `run_summary` conditional on clean shutdown completion or reserve it for replayability / integration evidence, make `entry_rejected` conditional on observed entry failures rather than healthy success paths, and make Mew-X `creator_candidate` conditional on non-empty candidate snapshots instead of a source-agnostic hard gate. The rule memo is now ready for approval, but `TASK-006` remains blocked until any approved validator rule implementation is actually applied.

`TASK-005-VALIDATOR-RULE-IMPLEMENT` re-verified latest GitHub-visible Vexter `main` at merged PR `#19` commit `db59c2edc099c4e794d8e0ac177cedb6f6d57d2d`, then applied the approved conditional validator treatments inside `vexter/comparison/validator.py` without changing Dexter or Mew-X source logic. The promoted pair `task005-pass-grade-pair-20260325T180027Z` reclassified to `pass/pass` with `winner_mode: derived`, while the confirmatory pair narrowed to a single Mew-X `candidate_rejected` residual. `TASK-006` therefore became ready for downstream replay validation.

`TASK-006-REPLAY-VALIDATION` re-verified latest GitHub-visible Vexter `main` at merged PR `#20` commit `6f2f50cc14dddfa52e6632db1dcbb98dbdb8a668` on `2026-03-25T19:47:52Z`, kept Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and kept frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. The promoted and confirmatory matched pairs were re-run through `validate`, `derive-metrics`, and `build-pack` on the current comparison layer. The promoted comparable pack remained `pass/pass` with `winner_mode: derived` and was accepted as downstream replay input, while the confirmatory pack stayed `pass/partial` with only a narrow Mew-X `candidate_rejected` residual. Both promoted replayability grades remained `partial`, so TASK-006 is now in progress rather than fully closed: the next step is replay deepening and live-versus-replay gap measurement, not another comparable-pack intake decision.

`TASK-006-REPLAY-DEEPENING` re-verified latest GitHub-visible Vexter `main` at merged PR `#21` commit `d7845b665afc912da593f2601e6a3d39524964d0` on `2026-03-25T20:05:57Z`, kept Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and kept frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. Starting from the accepted promoted comparable pack, Vexter reconstructed replay-mode evidence without changing source logic, re-ran `validate`, `derive-metrics`, and `build-pack` on the replay packages, and measured live-versus-replay gap. Dexter replay evidence now validates with `75.00%` closed-session coverage and a measurable `17.6205 pct` gap, including one uncovered live session and one large outlier, while Mew-X replay evidence validates with `100.00%` coverage and a `0.0000 pct` gap. The confirmatory Mew-X `candidate_rejected` miss remains only a narrow context note, and TASK-006 is now ready for the next replay-analysis step rather than blocked on replay evidence reconstruction.

`TASK-006-REPLAY-ANALYSIS` re-verified latest GitHub-visible Vexter `main` at merged PR `#22` commit `3db6d3e73c4a6d990e0fdf8fba33b31a3a436c5b` on `2026-03-25T20:35:55Z`, then interpreted the measured replay gap without changing Dexter or Mew-X source logic. The uncovered Dexter session `6aH...` is now explained as a source-surface omission: the run ended before that safe close could qualify for the `stagnant_mints` replay export path, so the missing coverage is not a Vexter linkage bug. The large Dexter outlier `AdCv...` is also explained: live Dexter sold on the `safe` path, but the replay surface only preserves a later stagnant terminal path, so replay deepening exits at a different price and reports a large negative gap. Mew-X still shows `0.0000 pct` gap on all six promoted closes because its exported session summaries match the live close metrics exactly, but that should be interpreted as summary fidelity rather than a full-path replay claim. The next narrow task is therefore a Dexter replay-surface fix, not downstream replay comparison yet.

`TASK-006-REPLAY-SURFACE-FIX` re-verified latest GitHub-visible Vexter `main` at merged PR `#23` commit `eaa5acc2f189d5bea5c55f9de059d8e6a8d36091` on `2026-03-25T21:05:35Z`, kept Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and kept frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. Instead of changing frozen source semantics, Vexter now augments the promoted Dexter package with source-faithful non-stagnant close-summary replay exports derived from the live event stream, keeps stagnant-mint exports as fallback, and prefers those close summaries during replay reconstruction. Re-running `validate`, `derive-metrics`, `build-pack`, and replay deepening on the promoted baseline lifts Dexter replay coverage from `75.00%` to `100.00%` and closes Dexter `live_vs_replay_gap_pct` from `17.6205` to `0.0000`, while Mew-X remains `100.00%` / `0.0000`. TASK-006 is now ready for downstream comparative analysis on the replay-surface-fixed promoted baseline.

`TASK-006-DOWNSTREAM-COMPARATIVE-ANALYSIS` re-verified latest GitHub-visible Vexter `main` at merged PR `#24` commit `2c8b654fde4b4fcff11f3ecca2e1f4d27ceb610d` on `2026-03-25T21:27:28Z`, kept Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and reverified frozen Mew-X `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. Using the replay-surface-fixed promoted baseline `task005-pass-grade-pair-20260325T180027Z`, it consolidated live and replay comparison interpretation without changing source logic. The promoted live pack stays `pass/pass` with `winner_mode: derived`, the repaired replay baseline stays `pass/pass` with `100.00%` coverage and `0.0000 pct` live-versus-replay gap for both sources, and the concrete scored-metric split is stable across live and replay: Dexter leads `candidate_count`, `signal_to_attempt_latency_ms`, `quote_vs_fill_slippage_bps`, `max_favorable_excursion_pct`, `realized_exit_pct`, and `stale_position_ratio`; Mew-X leads `attempt_to_fill_latency_ms`, `max_adverse_excursion_pct`, `time_to_peak_ms`, and `realized_vs_peak_gap_pct`; `fill_success_rate` and `replayability_grade` tie. The confirmatory pair still narrows to Mew-X `candidate_rejected` only and does not overturn the promoted baseline. The remaining caveat is scope: current replay parity is close-summary / `position_closed` realized-return fidelity rather than full path equivalence, and Dexter still shows one path-sensitive derived-metric drift (`realized_vs_peak_gap_pct` `20.5330` live vs `25.8042` replay) even with zero realized-return gap. No new evidence collection is justified on current facts; TASK-006 is now comparison-closeout ready, and the same artifact bundle is suitable for downstream research handoff.

`TASK-006-COMPARISON-CLOSEOUT` re-verified latest GitHub-visible Vexter `main` at merged PR `#25` commit `727589b40d26c453c2fef78dc5060aa1098c1aad` on `2026-03-25T21:43:44Z`, kept Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and kept frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It treats the promoted baseline `task005-pass-grade-pair-20260325T180027Z` plus the repaired replay comparison as the formal closeout source of truth, freezes the final comparison conclusion rather than reopening evidence collection, and packages the result for downstream research handoff. The final closeout conclusion stays component-level rather than scalar: live and replay both remain `winner_mode: derived` with the stable split Dexter `6`, Mew-X `4`, tie `2`; the confirmatory residual remains only Mew-X `candidate_rejected`; replay parity stays scoped to close-summary / `position_closed` realized-return fidelity; and the narrow Dexter `realized_vs_peak_gap_pct` drift plus Mew-X candidate-surface asymmetry remain explicit caveats. TASK-006 is now formally closed out, and the promoted baseline is the frozen comparison source of truth for downstream research or execution planning.

`TASK-006-RESEARCH-HANDOFF` re-verified latest GitHub-visible Vexter `main` at merged PR `#26` commit `9d235176e628e0fcbdcf2182ce83def25f6a6b02` on `2026-03-25T22:05:02Z`, kept Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and kept frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It promotes the comparison closeout outputs to the formal downstream research handoff package without changing source logic, validator rules, or frozen source pins. The promoted baseline remains `task005-pass-grade-pair-20260325T180027Z`; live and repaired replay both remain `winner_mode: derived`; the stable scored split stays Dexter `6`, Mew-X `4`, tie `2`; replay coverage / gap stay `100.00% / 0.0000` for both sources; and the only confirmatory residual remains Mew-X `candidate_rejected`. The handoff package now explicitly captures the comparison conclusion, caveat scope, residual treatment, artifact surfaces to use directly, and the work downstream researchers do not need to repeat. TASK-006 comparison work can now be treated as fully handed off to downstream research, strategy, or execution-planning workstreams.

`TASK-007-DOWNSTREAM-RESEARCH-INTAKE` re-verified latest GitHub-visible Vexter `main` at merged PR `#27` commit `d8149e624b9682e2da55ac4bfcaa30fcaa3d94df` on `2026-03-25T22:17:31Z`, kept Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and kept frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It treats the completed `TASK-006` research handoff package as the formal downstream input surface, keeps comparison conclusions frozen, and converts them into an operating intake for research work without changing source logic, validator rules, or source pins. The intake now fixes the comparison/research boundary, the reusable artifact surfaces, the non-goals and rerun prohibitions, and the first hypothesis lanes: candidate generation versus quality conversion, execution timing versus retention tradeoffs, risk-containment versus exit-capture weighting, and path-sensitive retention scope. The next work should now be a hypothesis-specific research, strategy-planning, or execution-planning cut rather than any reopening of `TASK-006`.

`TASK-007-CANDIDATE-GENERATION-VS-QUALITY` re-verified latest GitHub-visible Vexter `main` at merged PR `#28` commit `4df259b90365787ac24085227fcc1b15b63d057d` on `2026-03-25T22:36:55Z`, kept Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and kept frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. Using only the promoted live/replay comparisons plus committed candidate snapshots, session summaries, and close-summary metrics, it found that Dexter's `candidate_count` edge is not just raw breadth: the `255 creator_candidate` surface narrows to `4` closes with median `34.8443 pct` favorable excursion, median `7.1641 pct` realized exit, `0.0 bps` slippage, and `0.00%` stale ratio, while Mew-X still exposes no comparable upstream candidate surface and closes with median `0.0 pct` realized exit and `100.00%` stale ratio. The bounded conclusion is `candidate_edge_supported` with `candidate_quality_bounded`, and the next recommended lane is `execution_timing_vs_retention` rather than strategy planning or more comparison work.

`TASK-007-EXECUTION-TIMING-VS-RETENTION` re-verified latest GitHub-visible Vexter `main` at merged PR `#29` commit `17af013b0383fd142e15932108c8b4da5447f1f7` on `2026-03-25T22:57:54Z`, kept Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and kept frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. Using only the promoted live/replay comparisons plus committed Mew-X session summaries and Dexter close-summary metrics, it found a real stage split rather than a scalar winner: Dexter keeps the upstream timing, slippage, realized-exit, and stale-control edge, while Mew-X keeps the post-attempt fill-speed, drawdown, and local-peak-retention edge. The bounded conclusion is `timing_retention_tradeoff_supported` with `tradeoff_bounded`, and the next recommended lane is `risk_containment_vs_exit_capture` rather than another generic timing cut or an immediate strategy-planning jump.

`TASK-007-RISK-CONTAINMENT-VS-EXIT-CAPTURE` re-verified latest GitHub-visible Vexter `main` at merged PR `#30` commit `052de6ccecef1461d2291fb4f0ef5fbf8883d548` on `2026-03-25T23:15:40Z`, kept Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and kept frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. Using only the promoted live/replay comparisons plus committed Mew-X session summaries and Dexter close-summary metrics, it isolated the bounded objective split inside the current comparison: Mew-X owns price-path drawdown containment and local-peak retention, while Dexter owns realized exit capture, tighter slippage, and stale-position avoidance. The resulting conclusion is `risk_exit_tradeoff_supported` with `tradeoff_bounded`, and the next recommended task is `strategy_planning` rather than further evidence collection.

`TASK-007-STRATEGY-PLANNING` re-verified latest GitHub-visible Vexter `main` at merged PR `#31` commit `0905ec8c991ca6046b13d0c326c3224c49709a2d` on `2026-03-25T23:36:30Z`, kept Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and kept frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It treats the comparison baseline as fixed and converts the confirmed `TASK-007` lane results into an objective-weighted planning surface instead of reopening comparison work. The source-faithful planning conclusion is now asymmetric but actionable: Dexter should anchor execution planning whenever upstream opportunity capture, low-slippage entry, realized exit capture, or stale-risk avoidance matter materially, while Mew-X should be selected only for strategies that explicitly prioritize fast post-attempt fills, drawdown containment, and local-peak retention even with timeout-shaped lifecycle behavior. Hybrid use is supported only as selective adoption or objective-conditioned portfolio split, not as proven source fusion. The resulting status is `strategy_plan_ready` with `strategy_plan_bounded`, and the next recommended task is `execution_planning`.

`TASK-007-EXECUTION-PLANNING` re-verified latest GitHub-visible Vexter `main` at merged PR `#32` commit `a214ff04f0662e250b01181551a8963ce8e80dd6` on `2026-03-25T23:52:42Z`, kept Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and kept frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It treats the strategy plan as the formal input surface and converts it into a bounded execution-level design surface without reopening comparison work. The source-faithful execution conclusion is: Dexter remains the default execution anchor whenever objective weights are mixed or unspecified, Mew-X remains a containment-first selective sleeve only when drawdown containment and local-peak retention are explicitly prioritized, and hybrid use stays limited to objective-conditioned route selection or portfolio split rather than in-run source fusion. The resulting status is `execution_plan_ready` with `execution_plan_bounded`, and the next recommended task is `implementation_planning`.

`TASK-007-IMPLEMENTATION-PLANNING` re-verified latest GitHub-visible Vexter `main` at merged PR `#33` commit `c6dfaee621e3eea08ceec1cfba7f0fe65a5918fc` on `2026-03-26T00:21:56Z`, kept Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and kept frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It treats the execution plan as the formal input surface and converts it into implementation-ready boundaries for planner/router responsibility, objective-profile config, Dexter-default versus Mew-X-selective sleeves, bounded portfolio split candidates, monitoring and kill-switch layers, and source-specific executor contracts without reopening comparison work or changing source logic. The resulting status is `implementation_plan_ready` with `implementation_plan_bounded`, and the next recommended task is `planner_router_interface_spec`.

`TASK-007-PLANNER-ROUTER-INTERFACE-SPEC` re-verified latest GitHub-visible Vexter `main` at merged PR `#34` commit `981f0977788cffd10de4bf78f14e5b430fbce4c1` on `2026-03-26T00:39:22Z`, kept Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and kept frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It treats the bounded implementation plan as the formal input surface and fixes the first source-faithful planner/router contract: one immutable execution plan must bind objective profile, sleeve route, budget policy, monitor profile, executor profile, and source pin before any executor starts; mixed or unspecified objectives still default to Dexter; Mew-X remains an explicit containment-first sleeve only; and mid-trade handoff, threshold transplant, trust-logic transplant, and fused-engine claims remain out of scope. The resulting status is `planner_router_spec_ready` with `planner_router_spec_bounded`, and the next recommended task is `concrete_planner_router_implementation_spec`.

`TASK-007-CONCRETE-PLANNER-ROUTER-IMPLEMENTATION-SPEC` re-verified latest GitHub-visible Vexter `main` at merged PR `#35` commit `2bec1badde788ef1c75251af2df17609643720fb` on `2026-03-26T01:03:31Z`, kept Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and kept frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It treats the immutable planner/router contract as the formal input surface and turns it into a concrete implementation flow: deterministic config loading, fail-closed objective resolution, explicit sleeve selection, sleeve-scoped budget binding, atomic immutable plan emission, and `prepare`-then-`start` executor dispatch with no mid-trade rebinding. The resulting status is `concrete_planner_router_spec_ready` with `concrete_spec_bounded`, and the next recommended task is `concrete_planner_router_code_spec`.

`TASK-007-CONCRETE-PLANNER-ROUTER-CODE-SPEC` re-verified latest GitHub-visible Vexter `main` at merged PR `#36` commit `8713b9ba8de82accda48526fdff8732531d7b620` on `2026-03-26T01:24:01Z`, kept Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and kept frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It converts the concrete planner flow into a code-ready package split under `vexter/planner_router/`, fixing frozen dataclasses, protocol seams, deterministic module boundaries, and rollback semantics without changing frozen source logic. The resulting status is `planner_router_code_spec_ready` with `code_spec_bounded`, and the next recommended task is `planner_router_code_implementation`.

`TASK-007-PLANNER-ROUTER-CODE-IMPLEMENTATION` starts from that fixed code-spec surface, re-verifies latest GitHub-visible Vexter `main` at merged PR `#37` commit `786221a664637925f790e89af0448fa04832426e` on `2026-03-26T01:43:05Z`, and lands the first working planner/router control-plane implementation under `vexter/planner_router/` with committed config in `config/planner_router/`. The implementation now covers deterministic config loading, objective resolution, explicit sleeve selection, sleeve-scoped budget binding, immutable batch plan emission, source-faithful executor adapter seams, and a `prepare`-then-`start` dispatch state machine with stop-all rollback on partial prepare or start failure. The bounded next task is `planner_router_integration_smoke`, while numeric monitor thresholds and executor transport details remain intentionally deferred.

`TASK-007-PLANNER-ROUTER-INTEGRATION-SMOKE` re-verified latest GitHub-visible Vexter `main` at merged PR `#38` commit `b1ceac48af931db0571a25ea197b1e4e5df52543` on `2026-03-26T02:10:03Z`, kept Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and kept frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It exercised the public `plan_request()` and `plan_and_dispatch()` entrypoints against committed config plus source-faithful stub adapters, proving config load, objective resolution, sleeve selection, budget binding, immutable plan emission, prepare/start sequencing, and stop-all rollback stay wired end to end without reopening comparison work. The resulting status is `planner_router_integration_smoke_passed` with `integration_smoke_bounded`, and the next recommended task is `planner_router_runtime_smoke`.

`TASK-007-PLANNER-ROUTER-RUNTIME-SMOKE` re-verified latest GitHub-visible Vexter `main` at merged PR `#39` commit `a293d6e9260bd092297140bc82469802d6fce115` on `2026-03-26T02:28:37Z`, kept Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and kept frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It keeps the planner/router control plane source-faithful while proving the bounded runtime seam: immutable batches remain stored in `planned`, the shortest runtime path is now evidenced as `planned -> starting -> running -> quarantined -> stopping -> stopped`, invalid runtime transitions fail closed with typed `invalid_status_transition` failures, and reverse-order stop-all rollback remains bounded to source-native stop reasons rather than cross-source rebinding. The resulting status is `planner_router_runtime_smoke_passed` with `runtime_smoke_bounded`, and the next recommended task is `monitor_killswitch_spec`.

`TASK-007-MONITOR-KILLSWITCH-SPEC` re-verified latest GitHub-visible Vexter `main` at merged PR `#40` commit `a4e7a3c74460dc711c3e2897ac31386272e91c27` on `2026-03-26T02:45:48Z`, kept Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and kept frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It turns the already-implemented planner/router and bounded runtime smoke into a source-faithful monitor/killswitch contract: monitor profiles remain immutable per-plan bindings, admission gates stay pre-emission and fail closed, timeout envelopes stay source-shaped with numerics deferred, quarantine remains sleeve-scoped, global halt freezes new planning and emits source-native stop requests only, and typed failure escalation stays on `FailureCode` / `FailureDetail` with source-native reasons carried as passthrough metadata. The resulting status is `monitor_killswitch_spec_ready` with `monitor_killswitch_spec_bounded`, and the next recommended task is `planner_router_livepaper_smoke`.

`TASK-007-PLANNER-ROUTER-LIVEPAPER-SMOKE` starts from latest GitHub-visible Vexter `main` at merged PR `#41` commit `0b174515c9c0deb04b70fd38c2c2882736a22273` on `2026-03-26T03:08:04Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It extends the fixed planner/router control plane onto source-faithful paper-like executor seams without changing source logic: live-paper admission still fails closed under active global halt, immutable `plan_request()` output now reaches Dexter `paper_live` and Mew-X `sim_live` stub handles with pinned commit and monitor binding context intact, and `plan_and_dispatch()` now has a bounded live-paper smoke path for `planned -> starting -> running -> quarantined -> stopping -> stopped` with sleeve-local quarantine plus `manual_latched_stop_all` propagation to active plans. The resulting status is `planner_router_livepaper_smoke_passed` with `livepaper_smoke_bounded`, and the next recommended task is `planner_router_executor_transport_spec`.

`TASK-007-PLANNER-ROUTER-EXECUTOR-TRANSPORT-SPEC` starts from latest GitHub-visible Vexter `main` at merged PR `#42` commit `23e6f354bf261935639941d541227b0c3f7a8435` on `2026-03-26T03:27:34Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It fixes the bounded transport contract between the immutable planner/router control plane and the source-faithful executor runtimes without changing source logic: `PlanStore` remains pre-runtime persistence, `ExecutorRegistry` remains in-process adapter lookup, `ExecutorAdapter` owns the real bridge to source-native runtime processes, `StatusSink` remains normalized runtime fan-in, `prepare` becomes the only full-plan command, `handle_id` becomes the stable transport identity, `prepare/start/stop` are idempotent, and poll-first status reconciliation plus reverse-order stop confirmation are fixed while wire choice and numeric timing constants remain deferred. The resulting status is `planner_router_executor_transport_spec_ready` with `executor_transport_spec_bounded`, and the next recommended task is `planner_router_executor_transport_implementation`.

`TASK-007-PLANNER-ROUTER-EXECUTOR-TRANSPORT-IMPLEMENTATION` starts from latest GitHub-visible Vexter `main` at merged PR `#43` commit `64ffe4a58dd9ff9e5ef707d77b59f392da9e02e0` on `2026-03-26T03:48:25Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It turns the fixed transport contract into concrete bounded code under `vexter/planner_router/`: handle-based envelopes, source-faithful Dexter and Mew-X adapters, planner-owned in-memory `PlanStore` and `StatusSink` implementations, idempotent `prepare/start/stop`, and poll-first stop confirmation now exist behind the unchanged protocol seams, while source logic and comparison conclusions remain frozen. The resulting status is `planner_router_executor_transport_implemented` with `executor_transport_implementation_bounded`, and the next recommended task is `transport_runtime_smoke`.

`TASK-007-TRANSPORT-RUNTIME-SMOKE` starts from latest GitHub-visible Vexter `main` at merged PR `#44` commit `ae1dd7ddc3ffbae893887baf042ea5e5d53bc5f3` on `2026-03-26T11:00:22Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It runtime-smokes the concrete handle-based transport without changing source logic: source-faithful Dexter `paper_live` and Mew-X `sim_live` handles now prove bounded `prepare -> start -> status -> stop -> snapshot` progression, duplicate command idempotency, poll-first status reconciliation, snapshot-backed stop confirmation, transport-originated failure normalization, and reverse-order rollback stop fanout on the implemented surface. The resulting status is `transport_runtime_smoke_passed` with `transport_runtime_smoke_bounded`, and the next recommended task is `transport_livepaper_smoke`.

`TASK-007-TRANSPORT-LIVEPAPER-SMOKE` starts from latest GitHub-visible Vexter `main` at merged PR `#45` commit `b43f983848c462757a5b91e018f3200f28ec6b2b` on `2026-03-26T11:32:49Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It pushes the same bounded transport through the approved live-paper seam without changing source logic: immutable live-paper overlay batches now hand off directly into Dexter `paper_live` and Mew-X `sim_live` transport handles, live-paper dispatch can reconcile pushed quarantine under poll-first status selection, reverse-order `manual_latched_stop_all` still propagates across active handles with snapshot-backed terminal confirmation, and transport-originated `status_timeout` still normalizes cleanly on the live-paper request path. The resulting status is `transport_livepaper_smoke_passed` with `transport_livepaper_smoke_bounded`, and the next recommended task is `transport_livepaper_observability_smoke`.

`TASK-007-TRANSPORT-LIVEPAPER-OBSERVABILITY-SMOKE` starts from latest GitHub-visible Vexter `main` at merged PR `#46` commit `4df93f34a9f0cc483a38c31c2906bb6b43e5102f` on `2026-03-26T11:55:02Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It keeps the same source-faithful live-paper seam and proves the missing observability layer without changing source logic: immutable handoff metadata remains traceable into prepared handles, the shared status sink fans Dexter and Mew-X lifecycle snapshots into one visible stream, first-vs-duplicate ack metadata stays observable across prepare/start/stop, quarantine reason plus reverse-order `manual_latched_stop_all` propagation remain visible end to end, and transport-originated `status_timeout` still preserves normalized failure detail with source-faithful rollback snapshot context. The resulting status is `transport_livepaper_observability_smoke_passed` with `transport_livepaper_observability_smoke_bounded`, and the next recommended task is `transport_livepaper_observability_runtime`.

`TASK-007-TRANSPORT-LIVEPAPER-OBSERVABILITY-RUNTIME` starts from latest GitHub-visible Vexter `main` at merged PR `#48` commit `085f2692bf150431dd39365b6c954cf069e18617` on `2026-03-26T12:29:56Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It keeps the same source-faithful live-paper seam and carries the already-proved observability surface through a runtime-oriented path starting at `plan_and_dispatch()`: immutable handoff metadata remains traceable into runtime-created handles, the shared status sink keeps Dexter and Mew-X lifecycle snapshots visible across runtime follow-up, first-vs-duplicate ack metadata remains observable after runtime has started, quarantine reason plus reverse-order `manual_latched_stop_all` propagation remain visible end to end, and transport-originated `status_timeout` still preserves normalized failure detail with source-faithful rollback snapshot context. The resulting status is `transport_livepaper_observability_runtime_passed` with `transport_livepaper_observability_runtime_bounded`, and the next recommended task is `transport_livepaper_observability_hardening`.

`TASK-007-TRANSPORT-LIVEPAPER-OBSERVABILITY-HARDENING` starts from latest GitHub-visible Vexter `main` at merged PR `#49` commit `05ce9f2889da68cdb7336b667e6ad3adf8f5884b` on `2026-03-26T12:53:16Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It hardens the same source-faithful live-paper seam without changing source logic: prepared handles now retain immutable handoff metadata directly, `STARTING` and `FAILED` sink snapshots keep plan and handle context instead of dropping it at lifecycle boundaries, push-promoted quarantine keeps both poll and push detail, and snapshot-backed terminal detail plus rejection/failure detail preserve the same ack-history and source-faithful metadata surface across runtime follow-up. The resulting status is `transport_livepaper_observability_hardening_passed` with `transport_livepaper_observability_hardening_bounded`, and the next recommended task is `transport_livepaper_observability_regression_pack`.

`TASK-007-TRANSPORT-LIVEPAPER-OBSERVABILITY-REGRESSION-PACK` starts from latest GitHub-visible Vexter `main` at merged PR `#52` commit `64d0670c328f446d1caddda0e2d9c81534ef8787` on `2026-03-26T13:38:30Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It locks the hardened live-paper observability seam into durable transport regression coverage without changing source logic: immutable handoff metadata continuity, handle lifecycle continuity, status sink fan-in, ack-history retention, quarantine reason completeness, reverse-order `manual_latched_stop_all` propagation, snapshot-backed terminal detail, and normalized failure detail passthrough are now fixed as regression surfaces. The resulting status is `transport_livepaper_observability_regression_pack_passed` with `transport_livepaper_observability_regression_pack_bounded`, and the next recommended task is `transport_livepaper_observability_ci_gate`.

`TASK-007-TRANSPORT-LIVEPAPER-OBSERVABILITY-CI-GATE` starts from latest GitHub-visible Vexter `main` at merged PR `#52` commit `64d0670c328f446d1caddda0e2d9c81534ef8787` on `2026-03-26T13:38:30Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It promotes the fixed regression surface into an always-on CI gate without changing source logic: the source-faithful Dexter `paper_live` and Mew-X `sim_live` seam is now grouped under `transport_livepaper_observability_ci_gate`, each guarded observability axis fails under a distinct regression test name, and workflow proof output makes CI failures point directly to the broken surface. The resulting status is `transport_livepaper_observability_ci_gate_passed` with `transport_livepaper_observability_ci_gate_bounded`, and the next recommended task is `transport_livepaper_observability_watchdog`.

`TASK-007-TRANSPORT-LIVEPAPER-OBSERVABILITY-REGRESSION-PACK` starts from latest GitHub-visible Vexter `main` at merged PR `#52` commit `64d0670c328f446d1caddda0e2d9c81534ef8787` on `2026-03-26T13:38:30Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It converts the hardened live-paper observability seam into durable transport regressions without changing source logic: immutable handoff metadata continuity, handle lifecycle continuity, status sink fan-in, ack-history retention, quarantine reason completeness, reverse-order `manual_latched_stop_all` propagation, snapshot-backed terminal detail, and normalized failure detail are all locked into dedicated transport assertions. The resulting status is `transport_livepaper_observability_regression_pack_passed` with `transport_livepaper_observability_regression_pack_bounded`, and the next recommended task is `transport_livepaper_observability_ci_gate`.

`TASK-007-TRANSPORT-LIVEPAPER-OBSERVABILITY-CI-GATE` starts from latest GitHub-visible Vexter `main` at merged PR `#52` commit `64d0670c328f446d1caddda0e2d9c81534ef8787` on `2026-03-26T13:38:30Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It promotes the fixed regression surface into a standard CI gate without changing source logic: the guarded observability axes now run under an explicit `observability_ci_gate` suite before the rest of the repository, each surface fails under its own test name, and CI emits proof/status output for the gate directly in workflow logs. The resulting status is `transport_livepaper_observability_ci_gate_passed` with `transport_livepaper_observability_ci_gate_bounded`, and the next recommended task is `transport_livepaper_observability_watchdog`.

`TASK-007-TRANSPORT-LIVEPAPER-OBSERVABILITY-WATCHDOG` starts from latest GitHub-visible Vexter `main` at merged PR `#53` commit `64cd0e22c284c759a64939348a59c3152afc77dc` on `2026-03-26T14:14:55Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It adds a bounded watchdog layer on top of the CI-gated live-paper transport seam without changing source logic: the source-faithful Dexter `paper_live` and Mew-X `sim_live` transport metadata can now be checked for required field omission, handle lifecycle discontinuity, planned-versus-runtime metadata drift, partial status-sink fan-in, ack-history retention loss, missing quarantine or stop reasons, snapshot-backed terminal detail gaps, and normalized failure-detail passthrough loss. The resulting status is `transport_livepaper_observability_watchdog_passed` with `transport_livepaper_observability_watchdog_bounded`, and the next recommended task is `transport_livepaper_observability_watchdog_runtime`.

`TASK-007-TRANSPORT-LIVEPAPER-OBSERVABILITY-WATCHDOG-RUNTIME` starts from latest GitHub-visible Vexter `main` at merged PR `#56` commit `cc8996e0204b7dd149ebce85f1988d7a3abf90cd` on `2026-03-26T15:26:06Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It keeps the same source-faithful live-paper seam and verifies that the watchdog categories remain visible after runtime-oriented follow-up started through `plan_and_dispatch()`: duplicate ack state, quarantine promotion, reverse-order `manual_latched_stop_all`, snapshot-backed terminal detail, and normalized failure passthrough all stay watchdog-detectable without changing source logic. The resulting status is `transport_livepaper_observability_watchdog_runtime_passed` with `transport_livepaper_observability_watchdog_runtime_bounded`, and the next recommended task is `transport_livepaper_observability_watchdog_regression_pack`.

`TASK-007-TRANSPORT-LIVEPAPER-OBSERVABILITY-WATCHDOG-REGRESSION-PACK` starts from latest GitHub-visible Vexter `main` at merged PR `#56` commit `cc8996e0204b7dd149ebce85f1988d7a3abf90cd` on `2026-03-26T15:26:06Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It packages the bounded watchdog and watchdog-runtime surfaces into durable regression coverage without changing source logic: immutable handoff metadata continuity, handle lifecycle continuity, planned/runtime metadata drift detection, status sink fan-in, ack-history retention, quarantine reason completeness, reverse-order `manual_latched_stop_all` propagation, snapshot-backed terminal detail, and normalized failure-detail passthrough are now fixed as a standalone watchdog regression pack. The resulting status is `transport_livepaper_observability_watchdog_regression_pack_passed` with `transport_livepaper_observability_watchdog_regression_pack_bounded`, and the next recommended task is `transport_livepaper_observability_watchdog_ci_gate`.

`TASK-007-LIVEPAPER-OBSERVABILITY-SPEC` starts from latest GitHub-visible Vexter `main` at merged PR `#58` commit `10e23faa2a2428abae8206df963889392840919c` on `2026-03-26T16:29:07Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It converts the already CI-gated and watchdog-checked live-paper seam into one bounded operator-facing contract without changing source logic: immutable handoff metadata, handle lifecycle continuity, planned/runtime metadata continuity, status sink fan-in, ack-history retention, quarantine reason completeness, reverse-order `manual_latched_stop_all` propagation, snapshot-backed terminal detail, and normalized failure-detail passthrough are now specified as mandatory observability clauses with required fields, lifecycle semantics, failure-reporting rules, proof outputs, and operator interpretation. The resulting status is `livepaper_observability_spec_ready` with `livepaper_observability_spec_bounded`, and the next recommended task is `livepaper_observability_operator_runbook`.

`TASK-007-LIVEPAPER-OBSERVABILITY-OPERATOR-RUNBOOK` starts from latest GitHub-visible Vexter `main` at merged PR `#59` commit `4be21e4e52fbe686e585dea6b573efea759e0933` on `2026-03-26T16:54:45Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It turns the fixed observability contract into one bounded operator playbook without changing source logic: the repo now defines the read order for status/report/proof artifacts, the failure-surface priority for `omission`, `drift`, and `partial_visibility`, the interpretation of ack history, quarantine, reverse-order `manual_latched_stop_all`, snapshot-backed terminal detail, and normalized failure detail, and the halt-versus-continue decision boundary for live-paper observability incidents. The resulting status is `livepaper_observability_operator_runbook_ready` with `livepaper_observability_operator_runbook_bounded`, and the next recommended task is `livepaper_observability_shift_checklist`.

`TASK-007-LIVEPAPER-OBSERVABILITY-SHIFT-CHECKLIST` starts from latest GitHub-visible Vexter `main` at merged PR `#60` commit `ec46f051b41c3e140db09e808ef54e4d6e3d33ac` on `2026-03-26T17:18:32Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It turns the fixed observability contract plus operator runbook into one bounded shift procedure without changing source logic: the repo now defines what the operator checks before a shift starts, during periodic shift review, when `omission`, `drift`, or `partial_visibility` appears, and when the shift closes; it also fixes how `halt`, `quarantine`, and `continue` map onto reverse-order `manual_latched_stop_all`, quarantine completeness, snapshot-backed terminal detail, normalized failure detail, identity continuity, and sink-quality loss. The resulting status is `livepaper_observability_shift_checklist_ready` with `livepaper_observability_shift_checklist_bounded`, and the next recommended task is `livepaper_observability_shift_handoff_template`.

`TASK-007-LIVEPAPER-OBSERVABILITY-SHIFT-HANDOFF-TEMPLATE` starts from latest GitHub-visible Vexter `main` at merged PR `#62` commit `3b595faacf316aca89655b6e1ffcf7568478763e` on `2026-03-26T17:53:19Z`, keeps the completed checklist lane visible at PR `#61`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It turns the fixed contract, runbook, and shift checklist into one bounded outgoing and incoming operator handoff record without changing source logic: the repo now defines the mandatory handoff faces for current status, proof/report pointers, `omission` / `drift` / `partial_visibility`, manual stop-all, quarantine, terminal snapshot, normalized failure detail, open questions, and next-shift priority checks, while also fixing the completeness rule that every required face must be explicit or marked `none`. The resulting status is `livepaper_observability_shift_handoff_template_ready` with `livepaper_observability_shift_handoff_template_bounded`, and the next recommended task is `livepaper_observability_shift_handoff_drill`.

`TASK-007-LIVEPAPER-OBSERVABILITY-SHIFT-HANDOFF-DRILL` starts from latest GitHub-visible Vexter `main` at merged PR `#63` commit `9c00b7917f80bfbc4c7a0334625dddc09d903aca` on `2026-03-26T18:26:01Z`, carries forward the completed handoff-template lane at the same merged commit, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It exercises the fixed contract, runbook, checklist, and handoff template as one bounded outgoing and incoming operator drill without changing source logic: the repo now includes a filled sample handoff where current status, proof/report pointers, `omission` / `drift` / `partial_visibility`, manual stop-all, quarantine, terminal snapshot, normalized failure detail, open questions, and next-shift priority checks are all explicit or marked `none`, so the incoming operator can read the handoff without additional inference. The resulting status is `livepaper_observability_shift_handoff_drill_passed` with `livepaper_observability_shift_handoff_drill_bounded`, and the next recommended task is `livepaper_observability_shift_handoff_ci_check`.

`TASK-007-LIVEPAPER-OBSERVABILITY-SHIFT-HANDOFF-CI-CHECK` starts from latest GitHub-visible Vexter `main` at merged PR `#65` commit `51cfec8b85b5020ba5f6d0f72dceb5c47c339c06` on `2026-03-26T19:30:46Z`, carries forward the completed handoff drill as the formal baseline, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It promotes the fixed shift handoff template and drill into one bounded CI gate without changing source logic: the repo now runs `scripts/run_livepaper_observability_shift_handoff_ci_check.sh` in validation, groups the handoff tests under a dedicated pytest marker, emits proof/report/status outputs for the gate, and fails immediately when any required handoff face drifts away from the canonical template or sample handoff. The resulting status is `livepaper_observability_shift_handoff_ci_check_passed` with `livepaper_observability_shift_handoff_ci_check_bounded`, and the next recommended task is `livepaper_observability_shift_handoff_watchdog`.

`TASK-007-LIVEPAPER-OBSERVABILITY-SHIFT-HANDOFF-WATCHDOG` starts from latest GitHub-visible Vexter `main` at merged PR `#65` commit `51cfec8b85b5020ba5f6d0f72dceb5c47c339c06` on `2026-03-26T19:30:46Z`, carries forward the fixed handoff template, drill sample, and CI-check outputs as the formal baseline, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It adds a bounded handoff watchdog without changing source logic: the repo now evaluates the fixed handoff artifacts for required face omission, pointer shrinkage or ambiguity, implicit carry-over in place of explicit `false` or `none`, degraded manual stop-all and quarantine visibility, terminal snapshot and normalized failure detail loss, and incomplete open questions or next-shift priorities before the bundle is built. The resulting status is `livepaper_observability_shift_handoff_watchdog_passed` with `livepaper_observability_shift_handoff_watchdog_bounded`, and the next recommended task is `livepaper_observability_shift_handoff_watchdog_runtime`.

`TASK-007-LIVEPAPER-OBSERVABILITY-SHIFT-HANDOFF-WATCHDOG-RUNTIME` starts from latest GitHub-visible Vexter `main` at merged PR `#66` commit `2cd19d8ddbd5ef4918b41536494e10d4f2a9c125` on `2026-03-26T20:06:25Z`, carries forward the completed handoff watchdog lane plus the existing transport watchdog runtime proof as the formal runtime baseline, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It keeps the same bounded handoff watchdog effective on a runtime-shaped follow-up handoff without changing source logic: the repo now emits and validates a current runtime handoff where current status, proof/report pointers, `omission` / `drift` / `partial_visibility`, manual stop-all, quarantine, terminal snapshot, normalized failure detail, open questions, and next-shift priority checks remain explicit and watchdog-detectable on the runtime path. The resulting status is `livepaper_observability_shift_handoff_watchdog_runtime_passed` with `livepaper_observability_shift_handoff_watchdog_runtime_bounded`, and the next recommended task is `livepaper_observability_shift_handoff_watchdog_regression_pack`.

`TASK-007-LIVEPAPER-OBSERVABILITY-SHIFT-HANDOFF-WATCHDOG-REGRESSION-PACK` starts from latest GitHub-visible Vexter `main` at merged PR `#67` commit `7ab737a306ceca06193044d384e6b17d371838e2` on `2026-03-26T21:34:46Z`, carries forward the completed handoff-watchdog runtime lane as the formal baseline, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It packages the fixed handoff-watchdog and runtime-follow-up handoff-watchdog conclusions into one durable regression pack without changing source logic: the repo now freezes current status, proof/report pointers, `omission` / `drift` / `partial_visibility`, manual stop-all, quarantine, terminal snapshot, normalized failure detail, open questions, and next-shift priority checks across the baseline handoff, runtime follow-up handoff, and the current regression-pack handoff itself. The resulting status is `livepaper_observability_shift_handoff_watchdog_regression_pack_passed` with `livepaper_observability_shift_handoff_watchdog_regression_pack_bounded`, and the next recommended task is `livepaper_observability_shift_handoff_watchdog_ci_gate`.

`TASK-007-LIVEPAPER-OBSERVABILITY-SHIFT-HANDOFF-WATCHDOG-CI-GATE` starts from latest GitHub-visible Vexter `main` at merged PR `#68` commit `32133ff2acd233e0873d3de5817f2b31acafe809` on `2026-03-26T22:13:43Z`, keeps the merged regression-pack lane as the formal baseline, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It promotes the fixed handoff watchdog, handoff watchdog runtime, and handoff watchdog regression pack into an always-on CI gate without changing source logic: the repo now groups the fixed handoff watchdog suites under one fail-fast lane, emits proof/status/report output that points directly at the broken handoff face, and keeps current status, proof/report pointers, `omission` / `drift` / `partial_visibility`, manual stop-all, quarantine, terminal snapshot, normalized failure detail, open questions, and next-shift priority checks continuously enforced before the proof bundle is built. The resulting status is `livepaper_observability_shift_handoff_watchdog_ci_gate_passed` with `livepaper_observability_shift_handoff_watchdog_ci_gate_bounded`, and the next recommended task is `transport_livepaper_observability_acceptance_pack`.

`PATTERN-A-DEMO-EXECUTOR-CUTOVER` starts from latest GitHub-visible Vexter `main` at merged PR `#69` merge commit `289eb1bc87a8db9a9801b9b0583a8dbc6b17a5b3` on `2026-03-26T23:03:14Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It explicitly stops further observability deepening and fixes the shortest Pattern A cutover boundary instead: existing transport and handoff watchdog CI gates are treated as sufficient, the source-faithful seam remains Dexter `paper_live` / Mew-X `sim_live`, the first real demo target is Dexter only, and the minimal executor boundary is narrowed to adapter-owned demo submit / cancel / status / fill / stop-all behind the existing `ExecutorAdapter` lifecycle. The resulting status is `pattern_a_demo_executor_cutover_ready` with `pattern_a_demo_executor_cutover_bounded`, and the next recommended task is `demo_executor_adapter_implementation`.

`DEMO-EXECUTOR-ADAPTER-IMPLEMENTATION` starts from latest GitHub-visible Vexter `main` at merged PR `#70` merge commit `ccea54002189eaa8f6885284c7ef63506bb470be` on `2026-03-26T23:42:15Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It lands the smallest concrete Dexter demo runtime behind the unchanged planner lifecycle: `DexterDemoExecutorAdapter` and `build_demo_executor_transport_registry(...)` now fix the first bounded real-demo path while leaving the broad source-faithful registry and frozen Mew-X `sim_live` path unchanged. The resulting status is `demo_executor_adapter_implemented`, and the next recommended task is `demo_forward_acceptance_pack`.

`DEMO-FORWARD-ACCEPTANCE-PACK` starts from latest GitHub-visible Vexter `main` at merged PR `#71` merge commit `5c1feb2561da7b16a17c5d03b71ff2bf895e20e4` on `2026-03-27T00:09:27Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It fixes the bounded forward acceptance surfaces required before the first supervised demo window: the repo now treats Dexter-only `paper_live`, `single_sleeve`, `dexter_default`, explicit allowlist, small lot, one active real demo plan max, one open position max, bounded operator supervision, current status/report/proof/handoff pointers, an operator checklist, and an abort / rollback matrix as the current source of truth while keeping funded live forbidden and Mew-X unchanged on `sim_live`. The resulting status is `demo_forward_acceptance_pack_ready` with `demo_forward_acceptance_pack_bounded`, and the next recommended task is `demo_forward_supervised_run`.


`DEMO-FORWARD-SUPERVISED-RUN` starts from latest GitHub-visible Vexter `main` at merged PR `#72` merge commit `a6ca623b01dbed4191c4be10c2bfe51534025cac` on `2026-03-27T00:32:14Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It promotes a bounded supervised forward-demo lane rather than another packaging layer: the repo now exercises the source-faithful `prepare / start / status / stop / snapshot` sequence through `DexterDemoExecutorAdapter`, captures entry visibility, order / fill reconciliation, stop-all visibility, terminal snapshot detail, and handoff continuity as current source of truth, and then fail-closes the real-demo claim because external reference resolution, operator ownership, and venue connectivity remain repo-external and unverified. The resulting status is `demo_forward_supervised_run_blocked` with `demo_forward_supervised_run_fail_closed`, and the next recommended task is `supervised_run_retry_readiness`.


`DEMO-FORWARD-SUPERVISED-RUN-RETRY-READINESS` starts from latest GitHub-visible Vexter `main` at merged PR `#73` merge commit `aea530368fc5b59b87dd08a38c2629392ea8d706` on `2026-03-27T00:56:46Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It accepts the bounded supervised run as a blocked baseline and promotes retry readiness instead: the repo now fixes the current status/report/proof/handoff/checklist/prerequisite-matrix surfaces for the next retry, makes the missing external prerequisite set operator-visible, preserves the Dexter-only `paper_live` seam, and keeps funded live forbidden while leaving Mew-X unchanged on `sim_live`. The resulting status is `supervised_run_retry_readiness_blocked` with `supervised_run_retry_readiness_bounded`, and the next recommended task is `supervised_run_retry_gate`.


`DEMO-FORWARD-SUPERVISED-RUN-RETRY-GATE` starts from latest GitHub-visible Vexter `main` at merged PR `#74` merge commit `d06856cecf42c336af05902a1d932468c5d280b0` on `2026-03-27T01:20:35Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It accepts retry readiness as the blocked baseline and promotes a fail-closed retry gate instead: the repo now fixes the current status/report/proof/handoff/checklist/decision-surface surfaces for retry execution entry, labels repo-visible refs as observed markers rather than confirmations, keeps the Dexter-only `paper_live` seam bounded, and keeps funded live forbidden while leaving Mew-X unchanged on `sim_live`. The resulting status is `supervised_run_retry_gate_blocked` with `supervised_run_retry_gate_bounded`, and the current recommended step remains `supervised_run_retry_gate` until the gate can honestly pass.


`DEMO-FORWARD-SUPERVISED-RUN-RETRY-GATE-INPUT-ATTESTATION` starts from latest GitHub-visible Vexter `main` at merged PR `#75` merge commit `b9e556ffc57592190fdffbcabf61ab168b8ed467` on `2026-03-27T01:50:11Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It accepts the bounded retry-gate lane as baseline and promotes one fail-closed input-attestation lane instead: the repo now fixes the current status/report/proof/handoff/checklist/decision-surface surfaces for who attests each required gate face, what is being attested, the minimum evidence shape, and when the attestation goes stale, keeps the Dexter-only `paper_live` seam, leaves Mew-X unchanged on `sim_live`, and keeps funded live forbidden while holding the lane blocked until every attestation face is explicit enough. The resulting status is `supervised_run_retry_gate_input_attestation_blocked` with `supervised_run_retry_gate_input_attestation_bounded`, the blocked current lane remains `supervised_run_retry_gate_input_attestation`, and the attestation-pass successor is `supervised_run_retry_execution`.


`DEMO-FORWARD-SUPERVISED-RUN-RETRY-GATE-ATTESTATION-AUDIT` starts from latest GitHub-visible Vexter `main` at merged PR `#76` merge commit `56dedfe86bf9f6252d1099775fa9506d7259e0da` on `2026-03-27T07:27:42Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It accepts the bounded retry-gate input-attestation lane as baseline and promotes one fail-closed attestation-audit lane instead: the repo now fixes the current status/report/proof/handoff/checklist/decision-surface surfaces for whether each required face is explicit, non-stale, and auditable enough to reopen retry-gate review, keeps the Dexter-only `paper_live` seam, leaves Mew-X unchanged on `sim_live`, and keeps funded live forbidden while holding the lane blocked until every face can be audited honestly. The resulting status is `supervised_run_retry_gate_attestation_audit_blocked` with `supervised_run_retry_gate_attestation_audit_bounded`, the blocked current lane remains `supervised_run_retry_gate_attestation_audit`, and the audit-pass successor is `supervised_run_retry_gate`.


`DEMO-FORWARD-SUPERVISED-RUN-RETRY-GATE-ATTESTATION-RECORD-PACK` starts from latest GitHub-visible Vexter `main` at merged PR `#77` merge commit `1a35c468e1d2944e98a2987427d5b7d688cb0cfc` on `2026-03-27T07:50:49Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It accepts the bounded retry-gate attestation-audit lane as baseline and promotes one fail-closed attestation record-pack lane instead: the repo now fixes the current status/report/proof/handoff/checklist/decision-surface surfaces for who owns each required record face, the minimum evidence locator shape, the freshness requirement, the stale condition, and what makes the face reviewable enough, while keeping the Dexter-only `paper_live` seam, leaving Mew-X unchanged on `sim_live`, and keeping funded live forbidden. The resulting status is `supervised_run_retry_gate_attestation_record_pack_blocked` with `supervised_run_retry_gate_attestation_record_pack_bounded`, the current lane is `supervised_run_retry_gate_attestation_record_pack`, the blocked next recommended step is `supervised_run_retry_gate_attestation_refresh`, and the pass successor is `supervised_run_retry_gate`.

`DEMO-FORWARD-SUPERVISED-RUN-RETRY-GATE-ATTESTATION-RECORD-PACK-REGENERATION` starts from latest GitHub-visible Vexter `main` at merged PR `#88` merge commit `1d43904d392eefdcc911f00102cdff62bce9deb2` on `2026-03-27T20:05:00Z`, keeps Dexter pinned at merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`, and keeps frozen Mew-X at `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a`. It accepts the bounded attestation-refresh lane as baseline and promotes one fail-closed attestation record-pack-regeneration lane instead: the repo now fixes the current status/report/proof/handoff/checklist/decision-surface surfaces for regeneration owner, regeneration trigger, minimum regenerated locator shape, freshness inheritance or reset, and what makes each regenerated face reviewable enough, and it adds one canonical non-secret external-evidence manifest, validator, and gap report that refresh and regeneration now consume together, while keeping the Dexter-only `paper_live` seam, leaving Mew-X unchanged on `sim_live`, and keeping funded live forbidden. The resulting status is `supervised_run_retry_gate_attestation_record_pack_regeneration_blocked` with `supervised_run_retry_gate_attestation_record_pack_regeneration_bounded`, the current lane is `supervised_run_retry_gate_attestation_record_pack_regeneration`, the blocked next recommended step is `supervised_run_retry_gate_attestation_refresh`, and the pass successor is `supervised_run_retry_gate`.
