対象スレッド: 既存 Vexter Codex スレッド（なければ新規）
作業系統: Vexter / Infra
次タスクID: CONCRETE-PLANNER-ROUTER-CODE-SPEC

GitHub-visible Vexter `main` は PR `#35` merged commit `2bec1badde788ef1c75251af2df17609643720fb` まで確認済みで、comparison baseline は `task005-pass-grade-pair-20260325T180027Z` のまま固定です。Dexter は merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`、Mew-X は frozen commit `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a` を維持しています。promoted live / repaired replay はどちらも `winner_mode: derived`、stable scored split は Dexter `6`, Mew-X `4`, tie `2`、Dexter / Mew-X の replay coverage / gap はどちらも `100.00% / 0.0000` を維持し、confirmatory residual は Mew-X `candidate_rejected` のみで promoted baseline を overturn しません。

この task では、新証拠収集や source logic 変更は行わず、TASK-007 planner/router interface contract を concrete implementation spec に落としました。結論は以下です。

- Key finding: `concrete_planner_router_spec_ready`
- Claim boundary: `concrete_spec_bounded`
- Decision: `next_spec_ready`

source-faithful concrete implementation conclusion:

- config package は `planner`, `objective_profiles`, `sleeves`, `budget_policies`, `monitor_profiles`, `executor_profiles` に固定し、load order を deterministic にした
- objective resolution は omitted intent を `mixed_default` に寄せるが、explicit unknown profile は reject する
- sleeve selection は explicit enablement と source match を必須にし、Mew-X を Dexter に silent fallback しない
- budget binding は sleeve-scoped immutable binding とし、Mew-X allocation は cap reference 付き explicit policy のみ許可する
- plan emission は `plan_batch` 単位で atomic にし、1 plan = 1 sleeve = 1 source の immutable record を出す
- dispatch は `prepare` 全件成功後にのみ `start` し、partial prepare failure 時は rollback する
- normalized lifecycle は `planned -> starting -> running -> quarantined -> stopping -> stopped|failed`

次に切る task は `concrete_planner_router_code_spec` を推奨します。config load / validation / route resolution / plan emission / dispatch state machine の順序がここで固定できたため、次は module 分割、data model、function boundary、test matrix へ落とすのが最短です。`monitor_killswitch_spec` はこの plan lifecycle にぶら下げればよく、`executor_boundary_spec` は method/status/pin ownership が既に固定済みなので優先度を下げられます。再実行してはいけないもの:

- promoted-baseline selection
- 新 live / replay evidence collection
- validator / source logic changes
- mid-trade source handoff や fused-engine 前提の設計
- numeric timeout / retry / heartbeat / cap value の先行確定
