対象スレッド: 既存 Vexter Codex スレッド（なければ新規）
作業系統: Vexter / Infra
次タスクID: TASK-007-MONITOR-KILLSWITCH-SPEC

GitHub-visible Vexter `main` は PR `#39` main commit `a293d6e9260bd092297140bc82469802d6fce115` まで確認済みで、comparison baseline は `task005-pass-grade-pair-20260325T180027Z` のまま固定です。Dexter は merged PR `#3` commit `ddeb18c0dd21fa3a15d4a6a85573428f7d7ae938`、Mew-X は frozen commit `dba3dc84f1e2d4efc90fa5a4561593edcc9dd37a` を維持しています。promoted live / replay はどちらも `winner_mode: derived`、stable scored split は Dexter `6`, Mew-X `4`, tie `2`、Dexter / Mew-X の replay coverage / gap はどちらも `100.00% / 0.0000` を維持し、confirmatory residual は Mew-X `candidate_rejected` のみで promoted baseline を overturn しません。

この task では、新証拠収集や source logic 変更は行わず、実装済み planner/router control plane の bounded runtime smoke を追加しました。結論は以下です。

- Key finding: `planner_router_runtime_smoke_passed`
- Claim boundary: `runtime_smoke_bounded`
- Decision: `monitor_killswitch_spec_ready`

fixed runtime-smoke conclusion:

- `tests/test_planner_router_runtime_smoke.py` を追加し、public entrypoint の `plan_request()` と `plan_and_dispatch()` を runtime-like stub adapters で smoke した
- `plan_request()` 側では immutable batch が store 済みのまま `planned -> starting -> running -> quarantined -> stopping -> stopped` を最短経路で辿れることを確認した
- `plan_and_dispatch()` 側では invalid runtime transition が typed `invalid_status_transition` failure と reverse-order stop-all rollback completion に落ちることを確認した
- quarantine / stop signaling は `StatusSnapshot.detail` と adapter `stop(reason=...)` の typed seam で表現され、planner 層に transport ownership を持ち込んでいない
- source-faithful executor seam は `PlanStore` / `ExecutorAdapter` / `ExecutorRegistry` / `StatusSink` のまま維持し、numeric monitor threshold と transport ownership は deferred のままにした
- validation は `pytest -q` で `51 passed`

次は `monitor_killswitch_spec` を優先してください。理由は、runtime wiring と rollback completion は bounded に証明できたため、次の最短リスクは quarantine が sleeve-local のままか、global halt へ escalte するかという monitor/killswitch policy にあるためです。`executor_boundary_spec` は runtime smoke 後には重複度が高く、`planner_router_livepaper_smoke` は killswitch policy を固めてからの方が安全です。再実行してはいけないもの:

- promoted-baseline selection
- 新 live / replay evidence collection
- validator / source logic changes
- numeric timeout / heartbeat / retry / cap value の先行確定
- planner 層への transport / trust logic の持ち込み
