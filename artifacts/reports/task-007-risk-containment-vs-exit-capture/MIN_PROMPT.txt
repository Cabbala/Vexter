対象スレッド: 既存 Vexter Codex スレッド（なければ新規）
作業系統: Vexter / Infra
継続タスクID: TASK-007-RISK-CONTAINMENT-VS-EXIT-CAPTURE

PR #30 merged main を基準に、TASK-007 の `risk_containment_vs_exit_capture` lane は完了しています。comparison baseline は `task005-pass-grade-pair-20260325T180027Z` のまま固定で、promoted live / repaired replay はどちらも `winner_mode: derived`、stable scored split は Dexter `6`, Mew-X `4`, tie `2` です。Dexter / Mew-X の replay coverage / gap はどちらも `100.00% / 0.0000` を維持し、confirmatory residual は Mew-X `candidate_rejected` のみで promoted baseline を overturn しません。

lane の bounded conclusion は以下です。

- Key finding: `risk_exit_tradeoff_supported`
- Claim boundary: `tradeoff_bounded`

source-faithful evidence:

- Mew-X は median `max_adverse_excursion_pct` `0.0 pct`、median `time_to_peak_ms` `20.5`、median `realized_vs_peak_gap_pct` `0.0` なので price-path containment / local-peak retention は Mew-X 優位
- Dexter は median `max_favorable_excursion_pct` `34.8443 pct`、median `realized_exit_pct` `7.1641 pct`、median slippage `0.0 bps` なので exit capture / realized upside は Dexter 優位
- `stale_position_ratio` は Dexter `0.00%`、Mew-X `100.00%` で、Dexter は `safe=2`, `malicious=2`、Mew-X は全 `6` 件が `inactivity_timeout`
- Mew-X の `peak_to_close_ms` は全件ほぼ `5s` に集中しており、retention 優位は local path の保持であって active lifecycle control ではありません

したがって、比較の本質は「Mew-X が price-path risk containment を取り、Dexter が realized exit capture と stale-risk avoidance を取る」という objective split です。universal winner はまだありませんが、次のボトルネックは evidence 不足ではなく objective weighting です。

次に切る task は `strategy_planning` を推奨します。`path_sensitive_retention_scope` は、より強い retention/path-equivalence claim を後で必要とする場合だけで十分です。`execution_planning` は objective weights が決まった後に切るべきです。再実行してはいけないもの:

- promoted-baseline selection
- 新 live / replay evidence collection
- validator / source logic changes
- replay claim の full path equivalence 化
