# Demo Forward Supervised Run Retry Gate Attestation Record Pack Regeneration Checklist

## Required Runtime Markers
- `DEXTER_DEMO_CREDENTIAL_SOURCE`
- `DEXTER_DEMO_VENUE_REF`
- `DEXTER_DEMO_ACCOUNT_REF`
- `DEXTER_DEMO_CONNECTIVITY_PROFILE`
- `DEXTER_DEMO_ALLOWED_SYMBOLS`
- `DEXTER_DEMO_DEFAULT_SYMBOL`
- `DEXTER_DEMO_ORDER_SIZE_LOTS`
- `DEXTER_DEMO_MAX_ORDER_SIZE_LOTS`
- `DEXTER_DEMO_MAX_OPEN_POSITIONS=1`
- `DEXTER_DEMO_WINDOW_MINUTES=15`
- `manual_latched_stop_all`
- terminal snapshot readability
- funded live forbidden

## Review Steps
1. Start at `artifacts/reports/demo-forward-supervised-run-retry-gate-attestation-record-pack-regeneration-status.md`.
2. Review `artifacts/reports/demo-forward-supervised-run-retry-gate-attestation-record-pack-regeneration-report.md`.
3. Review `artifacts/proofs/demo-forward-supervised-run-retry-gate-attestation-record-pack-regeneration-check.json`.
4. Carry continuity from `artifacts/reports/demo-forward-supervised-run-retry-gate-attestation-record-pack-regeneration/HANDOFF.md`.
5. Use `docs/demo_forward_supervised_run_retry_gate_attestation_record_pack_regeneration_decision_surface.md` as the single record-pack regeneration decision surface.
6. Keep one regeneration row per required face: credential source, venue ref, account ref, connectivity profile, operator owner, bounded start criteria, allowlist / symbol / lot reconfirmation, `manual_latched_stop_all` visibility, and terminal snapshot readability.
7. For every row, confirm regeneration owner, regeneration trigger, minimum regenerated locator shape, freshness inheritance or reset rule, and what makes the regenerated face reviewable enough are explicit.
8. For every row, record whether a current fresh-enough bounded-window locator is present and can seed the regenerated face without embedding secret material.
9. Hold the lane at `FAIL/BLOCKED` until every required face is regenerated, current, and reviewable enough to reopen retry-gate review honestly.

## Regeneration Faces
| Regeneration face | Regeneration owner | Regeneration trigger | Minimum regenerated locator shape | Freshness inheritance or reset rule | Reviewable enough when |
| --- | --- | --- | --- | --- | --- |
| `external_credential_source_face` | `named_operator_owner_outside_repo` | refresh before the credential reference changes or the bounded supervised window rolls forward; otherwise keep the face blocked for record-pack regeneration; regenerate the current record-pack face again whenever the credential reference changes or the bounded supervised window rolls forward or when the bounded supervised window moves.; regenerate the current record-pack face again whenever inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when the credential reference changes or the bounded supervised window rolls forward or when the locator cannot be reopened without secrets or when the bounded supervised window moves. | attestor name, reference label only, bounded window, verification timestamp, and a plain-language confirmation that the referenced credential source was resolved outside repo; plus one repo-visible fresh-enough verification timestamp inside the current bounded supervised window and a locator that can be reopened without exposing secrets; plus regenerated face name, regeneration timestamp, bundle-relative proof pointer, and one reviewer-readable locator path that reopens the same bounded-window evidence without exposing secrets; plus regenerated face name, regeneration timestamp, bundle-relative proof pointer, and one reviewer-readable locator path that reopens the same bounded-window evidence without exposing secrets | inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when the credential reference changes or the bounded supervised window rolls forward or when the locator cannot be reopened without secrets or when the locator cannot be reopened without secrets | the repo can point to a current attestation record for this face without exposing secret material and the located evidence is still fresh enough for the current bounded supervised window and the regenerated face points to the same bounded-window locator plus an explicit regeneration timestamp and reviewer-readable proof path and the regenerated face points to the same bounded-window locator plus an explicit regeneration timestamp and reviewer-readable proof path |
| `venue_ref_face` | `named_operator_owner_plus_venue_owner_outside_repo` | refresh before the venue reference changes, venue routing changes, or the supervised window is rescheduled; otherwise keep the face blocked for record-pack regeneration; regenerate the current record-pack face again whenever the venue reference changes, venue routing changes, or the supervised window is rescheduled or when the bounded supervised window moves.; regenerate the current record-pack face again whenever inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when the venue reference changes, venue routing changes, or the supervised window is rescheduled or when the locator cannot be reopened without secrets or when the bounded supervised window moves. | attestor names, venue reference label, bounded window, verification timestamp, and explicit match confirmation between the marker and the intended venue; plus one repo-visible fresh-enough verification timestamp inside the current bounded supervised window and a locator that can be reopened without exposing secrets; plus regenerated face name, regeneration timestamp, bundle-relative proof pointer, and one reviewer-readable locator path that reopens the same bounded-window evidence without exposing secrets; plus regenerated face name, regeneration timestamp, bundle-relative proof pointer, and one reviewer-readable locator path that reopens the same bounded-window evidence without exposing secrets | inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when the venue reference changes, venue routing changes, or the supervised window is rescheduled or when the locator cannot be reopened without secrets or when the locator cannot be reopened without secrets | the venue attestation is current, explicit, and reviewable without requiring the reviewer to infer the mapping and the located evidence is still fresh enough for the current bounded supervised window and the regenerated face points to the same bounded-window locator plus an explicit regeneration timestamp and reviewer-readable proof path and the regenerated face points to the same bounded-window locator plus an explicit regeneration timestamp and reviewer-readable proof path |
| `account_ref_face` | `named_operator_owner_outside_repo` | refresh before the account reference changes or a different demo account is selected for the window; otherwise keep the face blocked for record-pack regeneration; regenerate the current record-pack face again whenever the account reference changes or a different demo account is selected for the window or when the bounded supervised window moves.; regenerate the current record-pack face again whenever inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when the account reference changes or a different demo account is selected for the window or when the locator cannot be reopened without secrets or when the bounded supervised window moves. | attestor name, account reference label, bounded window, verification timestamp, and a statement that the external account binding was checked outside repo; plus one repo-visible fresh-enough verification timestamp inside the current bounded supervised window and a locator that can be reopened without exposing secrets; plus regenerated face name, regeneration timestamp, bundle-relative proof pointer, and one reviewer-readable locator path that reopens the same bounded-window evidence without exposing secrets; plus regenerated face name, regeneration timestamp, bundle-relative proof pointer, and one reviewer-readable locator path that reopens the same bounded-window evidence without exposing secrets | inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when the account reference changes or a different demo account is selected for the window or when the locator cannot be reopened without secrets or when the locator cannot be reopened without secrets | the account attestation makes the intended demo account explicit enough to reopen gate review and the located evidence is still fresh enough for the current bounded supervised window and the regenerated face points to the same bounded-window locator plus an explicit regeneration timestamp and reviewer-readable proof path and the regenerated face points to the same bounded-window locator plus an explicit regeneration timestamp and reviewer-readable proof path |
| `connectivity_profile_face` | `named_operator_owner_plus_connectivity_owner_outside_repo` | refresh before network routing changes, connectivity profile changes, or the verification timestamp is outside the current window; otherwise keep the face blocked for record-pack regeneration; regenerate the current record-pack face again whenever network routing changes, connectivity profile changes, or the verification timestamp is outside the current window or when the bounded supervised window moves.; regenerate the current record-pack face again whenever inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when network routing changes, connectivity profile changes, or the verification timestamp is outside the current window or when the locator cannot be reopened without secrets or when the bounded supervised window moves. | attestor names, connectivity profile label, verification timestamp, bounded window, and a short reachability result; plus one repo-visible fresh-enough verification timestamp inside the current bounded supervised window and a locator that can be reopened without exposing secrets; plus regenerated face name, regeneration timestamp, bundle-relative proof pointer, and one reviewer-readable locator path that reopens the same bounded-window evidence without exposing secrets; plus regenerated face name, regeneration timestamp, bundle-relative proof pointer, and one reviewer-readable locator path that reopens the same bounded-window evidence without exposing secrets | inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when network routing changes, connectivity profile changes, or the verification timestamp is outside the current window or when the locator cannot be reopened without secrets or when the locator cannot be reopened without secrets | the connectivity attestation is current enough that a gate reviewer can treat reachability as explicitly checked and the located evidence is still fresh enough for the current bounded supervised window and the regenerated face points to the same bounded-window locator plus an explicit regeneration timestamp and reviewer-readable proof path and the regenerated face points to the same bounded-window locator plus an explicit regeneration timestamp and reviewer-readable proof path |
| `operator_owner_face` | `demo_operator_lead_outside_repo` | refresh before the named owner changes or the supervised window changes; otherwise keep the face blocked for record-pack regeneration; regenerate the current record-pack face again whenever the named owner changes or the supervised window changes or when the bounded supervised window moves.; regenerate the current record-pack face again whenever inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when the named owner changes or the supervised window changes or when the locator cannot be reopened without secrets or when the bounded supervised window moves. | owner name or handle, verification timestamp, window label, and explicit ownership statement; plus one repo-visible fresh-enough verification timestamp inside the current bounded supervised window and a locator that can be reopened without exposing secrets; plus regenerated face name, regeneration timestamp, bundle-relative proof pointer, and one reviewer-readable locator path that reopens the same bounded-window evidence without exposing secrets; plus regenerated face name, regeneration timestamp, bundle-relative proof pointer, and one reviewer-readable locator path that reopens the same bounded-window evidence without exposing secrets | inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when the named owner changes or the supervised window changes or when the locator cannot be reopened without secrets or when the locator cannot be reopened without secrets | the owner attestation is explicit enough that gate recheck does not rely on unnamed responsibility and the located evidence is still fresh enough for the current bounded supervised window and the regenerated face points to the same bounded-window locator plus an explicit regeneration timestamp and reviewer-readable proof path and the regenerated face points to the same bounded-window locator plus an explicit regeneration timestamp and reviewer-readable proof path |
| `bounded_start_criteria_face` | `named_operator_owner_outside_repo` | refresh before the scheduled window moves or ownership for go/no-go or abort changes; otherwise keep the face blocked for record-pack regeneration; regenerate the current record-pack face again whenever the scheduled window moves or ownership for go/no-go or abort changes or when the bounded supervised window moves.; regenerate the current record-pack face again whenever inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when the scheduled window moves or ownership for go/no-go or abort changes or when the locator cannot be reopened without secrets or when the bounded supervised window moves. | planned start window, bounded window minutes, go/no-go owner, abort owner, verification timestamp, and explicit stop condition note; plus one repo-visible fresh-enough verification timestamp inside the current bounded supervised window and a locator that can be reopened without exposing secrets; plus regenerated face name, regeneration timestamp, bundle-relative proof pointer, and one reviewer-readable locator path that reopens the same bounded-window evidence without exposing secrets; plus regenerated face name, regeneration timestamp, bundle-relative proof pointer, and one reviewer-readable locator path that reopens the same bounded-window evidence without exposing secrets | inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when the scheduled window moves or ownership for go/no-go or abort changes or when the locator cannot be reopened without secrets or when the locator cannot be reopened without secrets | the bounded-start attestation makes timing and ownership explicit enough to reopen gate review and the located evidence is still fresh enough for the current bounded supervised window and the regenerated face points to the same bounded-window locator plus an explicit regeneration timestamp and reviewer-readable proof path and the regenerated face points to the same bounded-window locator plus an explicit regeneration timestamp and reviewer-readable proof path |
| `allowlist_symbol_lot_reconfirmed` | `named_operator_owner_outside_repo` | refresh before any guardrail value changes or the supervised window moves; otherwise keep the face blocked for record-pack regeneration; regenerate the current record-pack face again whenever any guardrail value changes or the supervised window moves or when the bounded supervised window moves.; regenerate the current record-pack face again whenever inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when any guardrail value changes or the supervised window moves or when the locator cannot be reopened without secrets or when the bounded supervised window moves. | attestor name, verification timestamp, allowlist values, lot-size values, position cap, and a plain-language reconfirmation note; plus one repo-visible fresh-enough verification timestamp inside the current bounded supervised window and a locator that can be reopened without exposing secrets; plus regenerated face name, regeneration timestamp, bundle-relative proof pointer, and one reviewer-readable locator path that reopens the same bounded-window evidence without exposing secrets; plus regenerated face name, regeneration timestamp, bundle-relative proof pointer, and one reviewer-readable locator path that reopens the same bounded-window evidence without exposing secrets | inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when any guardrail value changes or the supervised window moves or when the locator cannot be reopened without secrets or when the locator cannot be reopened without secrets | the guardrail attestation is current enough that gate recheck can treat the bounded demo envelope as explicitly reconfirmed and the located evidence is still fresh enough for the current bounded supervised window and the regenerated face points to the same bounded-window locator plus an explicit regeneration timestamp and reviewer-readable proof path and the regenerated face points to the same bounded-window locator plus an explicit regeneration timestamp and reviewer-readable proof path |
| `manual_latched_stop_all_visibility_reconfirmed` | `named_operator_owner_outside_repo` | refresh before the stop-all surface changes or the window advances without reconfirmation; otherwise keep the face blocked for record-pack regeneration; regenerate the current record-pack face again whenever the stop-all surface changes or the window advances without reconfirmation or when the bounded supervised window moves.; regenerate the current record-pack face again whenever inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when the stop-all surface changes or the window advances without reconfirmation or when the locator cannot be reopened without secrets or when the bounded supervised window moves. | attestor name, verification timestamp, referenced stop-all surface, and a plain-language visibility confirmation; plus one repo-visible fresh-enough verification timestamp inside the current bounded supervised window and a locator that can be reopened without exposing secrets; plus regenerated face name, regeneration timestamp, bundle-relative proof pointer, and one reviewer-readable locator path that reopens the same bounded-window evidence without exposing secrets; plus regenerated face name, regeneration timestamp, bundle-relative proof pointer, and one reviewer-readable locator path that reopens the same bounded-window evidence without exposing secrets | inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when the stop-all surface changes or the window advances without reconfirmation or when the locator cannot be reopened without secrets or when the locator cannot be reopened without secrets | the stop-all visibility attestation is current enough to reopen gate review without assuming prior visibility still holds and the located evidence is still fresh enough for the current bounded supervised window and the regenerated face points to the same bounded-window locator plus an explicit regeneration timestamp and reviewer-readable proof path and the regenerated face points to the same bounded-window locator plus an explicit regeneration timestamp and reviewer-readable proof path |
| `terminal_snapshot_readability_reconfirmed` | `named_operator_owner_outside_repo` | refresh before the snapshot format changes or the supervised window advances without reconfirmation; otherwise keep the face blocked for record-pack regeneration; regenerate the current record-pack face again whenever the snapshot format changes or the supervised window advances without reconfirmation or when the bounded supervised window moves.; regenerate the current record-pack face again whenever inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when the snapshot format changes or the supervised window advances without reconfirmation or when the locator cannot be reopened without secrets or when the bounded supervised window moves. | attestor name, verification timestamp, referenced snapshot surface, and a short readability confirmation; plus one repo-visible fresh-enough verification timestamp inside the current bounded supervised window and a locator that can be reopened without exposing secrets; plus regenerated face name, regeneration timestamp, bundle-relative proof pointer, and one reviewer-readable locator path that reopens the same bounded-window evidence without exposing secrets; plus regenerated face name, regeneration timestamp, bundle-relative proof pointer, and one reviewer-readable locator path that reopens the same bounded-window evidence without exposing secrets | inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when inherit freshness only while the underlying fresh locator remains current, reviewable, and inside the same bounded supervised window; reset the regenerated face to FAIL/BLOCKED when the snapshot format changes or the supervised window advances without reconfirmation or when the locator cannot be reopened without secrets or when the locator cannot be reopened without secrets | the snapshot-readability attestation is current enough that a gate reviewer can rely on terminal visibility without inference and the located evidence is still fresh enough for the current bounded supervised window and the regenerated face points to the same bounded-window locator plus an explicit regeneration timestamp and reviewer-readable proof path and the regenerated face points to the same bounded-window locator plus an explicit regeneration timestamp and reviewer-readable proof path |
