# Demo Forward Supervised Run Retry Gate Attestation Refresh Checklist

## Required Runtime Markers
- `DEXTER_DEMO_CREDENTIAL_SOURCE`
- `DEXTER_DEMO_VENUE_REF`
- `DEXTER_DEMO_ACCOUNT_REF`
- `DEXTER_DEMO_CONNECTIVITY_PROFILE`
- `DEXTER_DEMO_ALLOWED_SYMBOLS`
- `DEXTER_DEMO_DEFAULT_SYMBOL`
- `DEXTER_DEMO_ORDER_SIZE_LOTS`
- `DEXTER_DEMO_MAX_ORDER_SIZE_LOTS`
- `DEXTER_DEMO_MAX_OPEN_POSITIONS=1`
- `DEXTER_DEMO_WINDOW_MINUTES=15`
- `manual_latched_stop_all`
- terminal snapshot readability
- funded live forbidden

## Review Steps
1. Start at `artifacts/reports/demo-forward-supervised-run-retry-gate-attestation-refresh-status.md`.
2. Review `artifacts/reports/demo-forward-supervised-run-retry-gate-attestation-refresh-report.md`.
3. Review `artifacts/proofs/demo-forward-supervised-run-retry-gate-attestation-refresh-check.json`.
4. Review the canonical manifest template at `manifests/demo_forward_supervised_run_retry_gate_external_evidence_manifest.json`.
5. Review the canonical evidence preflight report at `artifacts/reports/demo-forward-supervised-run-retry-gate-evidence-preflight-report.md`.
6. Use the `Next Human Pass Checklist` section in `artifacts/reports/demo-forward-supervised-run-retry-gate-evidence-preflight-report.md` as the canonical fill order and false-path explanation.
7. Carry continuity from `artifacts/reports/demo-forward-supervised-run-retry-gate-attestation-refresh/HANDOFF.md`.
8. Use `docs/demo_forward_supervised_run_retry_gate_attestation_refresh_decision_surface.md` as the single attestation refresh decision surface.
9. Keep one refresh row per required face: credential source, venue ref, account ref, connectivity profile, operator owner, bounded start criteria, allowlist / symbol / lot reconfirmation, `manual_latched_stop_all` visibility, and terminal snapshot readability.
10. For every row, confirm refresh owner, refresh trigger, minimum fresh evidence locator shape, stale condition, and what makes the refreshed face usable for retry-gate review are explicit.
11. For every row, record whether a current fresh-enough bounded-window locator is present without embedding secret material.
12. Hold the lane at `FAIL/BLOCKED` until every required face is current, fresh enough, and usable enough to rerun the current record-pack regeneration honestly.

## Refresh Faces
| Refresh face | Refresh owner | Refresh trigger | Minimum fresh evidence locator shape | Stale condition | Usable for retry-gate review when |
| --- | --- | --- | --- | --- | --- |
| `external_credential_source_face` | `named_operator_owner_outside_repo` | refresh before the credential reference changes or the bounded supervised window rolls forward; otherwise keep the face blocked for record-pack regeneration | attestor name, reference label only, bounded window, verification timestamp, and a plain-language confirmation that the referenced credential source was resolved outside repo; plus one repo-visible fresh-enough verification timestamp inside the current bounded supervised window and a locator that can be reopened without exposing secrets | the credential reference changes or the bounded supervised window rolls forward | the repo can point to a current attestation record for this face without exposing secret material and the located evidence is still fresh enough for the current bounded supervised window |
| `venue_ref_face` | `named_operator_owner_plus_venue_owner_outside_repo` | refresh before the venue reference changes, venue routing changes, or the supervised window is rescheduled; otherwise keep the face blocked for record-pack regeneration | attestor names, venue reference label, bounded window, verification timestamp, and explicit match confirmation between the marker and the intended venue; plus one repo-visible fresh-enough verification timestamp inside the current bounded supervised window and a locator that can be reopened without exposing secrets | the venue reference changes, venue routing changes, or the supervised window is rescheduled | the venue attestation is current, explicit, and reviewable without requiring the reviewer to infer the mapping and the located evidence is still fresh enough for the current bounded supervised window |
| `account_ref_face` | `named_operator_owner_outside_repo` | refresh before the account reference changes or a different demo account is selected for the window; otherwise keep the face blocked for record-pack regeneration | attestor name, account reference label, bounded window, verification timestamp, and a statement that the external account binding was checked outside repo; plus one repo-visible fresh-enough verification timestamp inside the current bounded supervised window and a locator that can be reopened without exposing secrets | the account reference changes or a different demo account is selected for the window | the account attestation makes the intended demo account explicit enough to reopen gate review and the located evidence is still fresh enough for the current bounded supervised window |
| `connectivity_profile_face` | `named_operator_owner_plus_connectivity_owner_outside_repo` | refresh before network routing changes, connectivity profile changes, or the verification timestamp is outside the current window; otherwise keep the face blocked for record-pack regeneration | attestor names, connectivity profile label, verification timestamp, bounded window, and a short reachability result; plus one repo-visible fresh-enough verification timestamp inside the current bounded supervised window and a locator that can be reopened without exposing secrets | network routing changes, connectivity profile changes, or the verification timestamp is outside the current window | the connectivity attestation is current enough that a gate reviewer can treat reachability as explicitly checked and the located evidence is still fresh enough for the current bounded supervised window |
| `operator_owner_face` | `demo_operator_lead_outside_repo` | refresh before the named owner changes or the supervised window changes; otherwise keep the face blocked for record-pack regeneration | owner name or handle, verification timestamp, window label, and explicit ownership statement; plus one repo-visible fresh-enough verification timestamp inside the current bounded supervised window and a locator that can be reopened without exposing secrets | the named owner changes or the supervised window changes | the owner attestation is explicit enough that gate recheck does not rely on unnamed responsibility and the located evidence is still fresh enough for the current bounded supervised window |
| `bounded_start_criteria_face` | `named_operator_owner_outside_repo` | refresh before the scheduled window moves or ownership for go/no-go or abort changes; otherwise keep the face blocked for record-pack regeneration | planned start window, bounded window minutes, go/no-go owner, abort owner, verification timestamp, and explicit stop condition note; plus one repo-visible fresh-enough verification timestamp inside the current bounded supervised window and a locator that can be reopened without exposing secrets | the scheduled window moves or ownership for go/no-go or abort changes | the bounded-start attestation makes timing and ownership explicit enough to reopen gate review and the located evidence is still fresh enough for the current bounded supervised window |
| `allowlist_symbol_lot_reconfirmed` | `named_operator_owner_outside_repo` | refresh before any guardrail value changes or the supervised window moves; otherwise keep the face blocked for record-pack regeneration | attestor name, verification timestamp, allowlist values, lot-size values, position cap, and a plain-language reconfirmation note; plus one repo-visible fresh-enough verification timestamp inside the current bounded supervised window and a locator that can be reopened without exposing secrets | any guardrail value changes or the supervised window moves | the guardrail attestation is current enough that gate recheck can treat the bounded demo envelope as explicitly reconfirmed and the located evidence is still fresh enough for the current bounded supervised window |
| `manual_latched_stop_all_visibility_reconfirmed` | `named_operator_owner_outside_repo` | refresh before the stop-all surface changes or the window advances without reconfirmation; otherwise keep the face blocked for record-pack regeneration | attestor name, verification timestamp, referenced stop-all surface, and a plain-language visibility confirmation; plus one repo-visible fresh-enough verification timestamp inside the current bounded supervised window and a locator that can be reopened without exposing secrets | the stop-all surface changes or the window advances without reconfirmation | the stop-all visibility attestation is current enough to reopen gate review without assuming prior visibility still holds and the located evidence is still fresh enough for the current bounded supervised window |
| `terminal_snapshot_readability_reconfirmed` | `named_operator_owner_outside_repo` | refresh before the snapshot format changes or the supervised window advances without reconfirmation; otherwise keep the face blocked for record-pack regeneration | attestor name, verification timestamp, referenced snapshot surface, and a short readability confirmation; plus one repo-visible fresh-enough verification timestamp inside the current bounded supervised window and a locator that can be reopened without exposing secrets | the snapshot format changes or the supervised window advances without reconfirmation | the snapshot-readability attestation is current enough that a gate reviewer can rely on terminal visibility without inference and the located evidence is still fresh enough for the current bounded supervised window |
