# Codex Memory - Vexter Fixed Assumptions

- Project name is `Vexter`.
- Mac control-plane folder is `~/Projects/Vexter`.
- GitHub target repo is `Cabbala/Vexter`.
- Dexter and Mew-X remain separate repos during initial comparison phases.
- Windows host is reachable with `ssh win-lan`.
- Windows should hold runtime files, DBs, collector outputs, and replay data.
- Git operations must be done from Mac, not Windows.
- Future deliverables must stay in the format: minimal prompt + detailed tar.gz bundle.
- After every meaningful task, Codex should push Git changes and make status inspectable from GitHub.
- ChatGPT will use GitHub-visible state to guide next tasks.
